# Sentiment Analysis Bot - Telegram бот для анализа тональности

## 🚀 Быстрый старт

### 1. Клонируйте и настройте
```bash
git clone <ваш-репозиторий>
cd easy-bot

# Создайте .env файл (см. .env.example)
cp .env.example .env
# Отредактируйте .env файл, добавив ваш токен бота
```

### 2. Запустите через Docker (рекомендуется)
```bash
docker-compose up -d
```

Или вручную:
```bash
python -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
pip install -e .
python -m app.bot.bot
```

Для запуска API отдельно:
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

## ✨ Что делает бот
- 📝 **Анализирует текст** - определяет эмоциональную окраску (позитив/негатив/нейтрально)
- 📊 **Показывает точность** - процент уверенности предсказания
- 📈 **Статистика** - общая статистика использования
- 🔘 **Удобный интерфейс** - интерактивные кнопки в Telegram

## ⚙️ Настройка
Создайте файл `.env` в корне проекта на основе `.env.example`:
```env
# Обязательные параметры
BOT_TOKEN=ваш_токен_бота_от_BotFather

# Опциональные параметры
ADMIN_ID=123456789  # ID администратора
ML_MODEL_NAME=cointegrated/rubert-tiny-sentiment-balanced  # Модель для анализа
API_HOST=localhost  # Хост API
API_PORT=8000  # Порт API
API_RELOAD=false  # Автоперезагрузка при изменениях
LOG_LEVEL=INFO  # Уровень логирования
ENVIRONMENT=production  # Окружение (development/production)
```

## 🐳 Docker 
```bash
docker-compose up -d      # Запуск
docker-compose logs -f    # Просмотр логов
docker-compose ps         # Статус контейнеров
docker-compose down       # Остановка
```

## 📱 Использование
1. Найдите бота в Telegram
2. Отправьте команду `/start`
3. Пришлите любой текст для анализа (от 3 до 1000 символов)
4. Получите результат с тональностью и точностью

## 🧪 Тестирование
Запуск тестов API:
```bash
python -m pytest app/tests/_test.py -v
```

## 📁 Структура проекта
```
easy-bot/
├── app/                 # Основное приложение
│   ├── bot/             # Telegram бот
│   ├── core/            # Конфигурация
│   ├── tests/           # Тесты
│   ├── main.py          # FastAPI приложение
│   └── ml_service.py     # Сервис анализа тональности
├── .env.example         # Пример конфигурации
├── .dockerignore         # Игнорирование файлов для Docker
├── docker-compose.yml    # Конфигурация Docker Compose
├── README.md             # Документация
├── requirements.txt     # Зависимости
└── setup.py             # Конфигурация пакета
```

## 🐛 Если не работает
1. ✅ Проверьте токен в `.env`
2. ✅ Посмотрите логи: `docker-compose logs bot`
3. ✅ Для ручного запуска: `python -m app.bot.bot`
4. ✅ Проверьте интернет-соединение
5. ✅ Убедитесь, что все зависимости установлены

---

## **Готово к работе!** 
🎉 Бот отвечает на команды `/start`, `/help`, `/about` , `/stats`, `history`  и анализирует любой отправленный текст до 1000 символов.
